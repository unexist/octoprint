profiles:
  development:
    name: null
    default: true
    data:
      host: http://octopi.local

.base_request:
  headers:
    accept: application/json
    x-api-key: "{{ env('API_KEY') }}"

requests:
  state:
    $ref: "#/.base_request"
    method: GET
    url: '{{ host }}/api/printer'
